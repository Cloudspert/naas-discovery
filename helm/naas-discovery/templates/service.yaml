apiVersion: v1
kind: Service
metadata:
  name: {{ include "naas-discovery.fullname" . }}
  labels:
    {{- include "naas-discovery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "naas-discovery.selectorLabels" . | nindent 4 }}
