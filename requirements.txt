# API framework
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0

# Outbound HTTP (Prometheus queries, cluster /healthz checks)
httpx==0.28.1

# PromQL query templating
Jinja2==3.1.6

# Registry / telemetry / auth config files
PyYAML==6.0.3
